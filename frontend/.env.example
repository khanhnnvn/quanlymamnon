# URL nội bộ của backend Go API (chỉ dùng ở server-side cho next.config.js rewrites).
# Trình duyệt luôn gọi same-origin /api/v1/... nên không cần biến NEXT_PUBLIC_* nào khác.
API_INTERNAL_URL=http://127.0.0.1:8097
